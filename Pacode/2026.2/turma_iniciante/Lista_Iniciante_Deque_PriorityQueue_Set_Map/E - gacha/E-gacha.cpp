#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int quantidade;
    cin >> quantidade;
    for(int i = 0; i < quantidade; i++){
        string nome; 
        
    }

    return 0;
}
