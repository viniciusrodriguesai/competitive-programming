#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int quantidade;
    cin >> quantidade;
    set <string> palavras;
    for(int i = 0; i < quantidade; i++){
        string nome; 
        cin >> nome;
    }

    return 0;
}
