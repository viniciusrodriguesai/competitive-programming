#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int numero;
    string comando;
    priority_queue<int> pq;
    cin >> comando;
    while(comando != "end" ){
            if(comando == "insert"){
                        cin >> comando;

            }
    }
    return 0;
}
