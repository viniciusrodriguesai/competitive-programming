/*
 * Pessoa Mais Velha com Pair
 *
 * Vamos praticar o uso do pair no Code::Blocks!
 *
 * Faça um programa que:
 * - leia a idade e o nome de duas pessoas (primeiro a idade, depois o nome);
 * - guarde os dados de cada uma em um pair<int, string> separado;
 * - use um if para comparar as idades (first) e descubra quem é a pessoa
 *   mais velha;
 * - imprima o nome da pessoa mais velha.
 *
 * Entrada de teste:
 * 25 Batman
 * 35 Coringa
 *
 * Saída esperada:
 * Coringa
 */
#include <bits/stdc++.h>
using namespace std;

int main() {

    pair<int,string>criatura1;
    pair<int,string>criatura2;
    cin >> criatura1 >> criatura2;

    return 0;
}
